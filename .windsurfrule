# flutter-template 开发规则

适用于整个 `flutter-template` 仓库。

## 核心要求

1. Windows 兼容优先
- 涉及滚动、拖拽、滑动、缩放、悬停、滚轮等交互时，必须优先考虑 Windows 桌面端兼容性。
- 不要只按移动端触屏手势实现；同时考虑鼠标滚轮、触控板、鼠标拖拽、点击命中区域。
- 涉及 `GestureDetector`、`Listener`、`ScrollConfiguration`、`PointerDeviceKind`、`MouseRegion`、`RawGestureDetector`、分页滑动、视频手势时，必须评估 Windows 下是否可用。
- 如果某交互在移动端和 Windows 端行为不同，优先保证 Windows 可用，再兼顾移动端体验。
- 新增滚动行为时，避免依赖只在移动端体验良好的默认物理反馈；必要时显式配置桌面端滚动行为。

2. 长组件必须拆分
- 新写组件如果已经明显偏长、职责混杂、嵌套层级过深，必须拆分。
- 优先把可复用或可独立理解的 UI 子块拆到当前页面同级目录下的 `./widgets` 目录。
- 页面文件应尽量保留页面编排、状态组织、事件分发，不要把大量 UI 细节长期堆在一个文件里。
- 拆分后的组件命名必须有业务语义，不允许 `item1`、`boxWidget`、`testCard` 之类命名。
- 纯展示组件优先做成小而清晰的 widget，避免把副作用、请求、复杂状态塞进展示组件。

3. `flutter analyze` 必须清理 warning
- 每次完成代码改动后，必须以 `flutter analyze` 为基准检查质量。
- 不允许明知存在 warning 还直接交付。
- warning 必须一起修掉，不要只修 error。
- 不要通过随意添加 ignore、关闭 lint、降低规则来掩盖问题；只有在确有必要且理由充分时，才允许极少量定点 ignore。

4. 图标资源优先级
- 图标优先使用 `flutter-template/assets/icons` 下已有资源。
- 在新增图标前，先检查现有资源是否可复用。
- 只有现有资源无法满足需求时，才允许新增图标或退回使用框架内置图标。
- 如果同时存在资源图标和内置 icon，优先使用项目资产图标以保持视觉统一。

5. 禁止低级 lint 问题
- 不允许提交明显基础级别的 Dart/Flutter 问题。
- 例如：
  - `Use 'const' for final variables initialized to a constant value`
  - 明明可 `const` 却没写
  - 未使用 import
  - 未使用局部变量
  - 死代码
  - 可空/非空写法粗糙
  - 明显多余的字符串插值
  - 可直接简化的布尔表达式
- 写代码时主动规避，不要等分析器报了再回头补。

## Flutter / Dart 代码风格

6. 保持强类型
- 避免 `dynamic` 和裸 `Map<String, dynamic>` 承载核心业务数据。
- 业务数据优先使用明确 model class。
- API 层返回值必须有清晰类型，不要把裸响应直接抛给 UI。

7. 职责分层清晰
- 页面层负责布局编排、交互组织、状态消费。
- 通用逻辑放 `utils` / `services` / `api` / `model` 等合适位置。
- 不要把初始化、副作用、网络请求、复杂转换逻辑堆在单个 Widget build 内。

8. 命名必须语义化
- 类名使用 `UpperCamelCase`。
- 变量、方法、字段使用 `lowerCamelCase`。
- 禁止长期保留 demo/占位命名，例如 `testPage`、`data1`、`path1`、`widget2`。

9. 优先写可维护代码
- 修改现有功能时优先复用已有抽象，不要重复造轮子。
- 如果一个逻辑已经在别处存在，优先抽公共方法或公共组件，而不是复制粘贴。
- 超过一个屏幕高度的大段 build UI，要主动考虑拆分。

10. import 与文件结构规范
- import 必须放文件顶部。
- 不要在文件中部插入 import。
- 新文件落位要符合项目分层；页面组件优先放对应页面目录下的 `widgets` 子目录。

## 交互与布局规则

11. 键盘与输入框处理要稳定
- 涉及输入框、键盘弹出、底部 inset、滚动联动时，必须避免页面抖动、慢速顶起、反复重排。
- 优先避免不必要的整页 `resizeToAvoidBottomInset` 联动。
- 涉及 `ScrollView + TextField` 时，要检查聚焦后的自动滚动行为是否合理，尤其是真机和 Windows 场景。

12. 视频/列表/手势场景要考虑多端
- 短视频页、轮播页、分页页、长列表页的交互实现，必须同时评估：
  - 触屏滑动
  - 鼠标滚轮
  - 触控板滚动
  - Windows 下的点击和 hover
- 手势冲突要优先解决，不要让桌面端出现无法滚动、误触、抢事件的问题。

## 提交前自检

每次完成改动后，至少自检以下内容：
- 是否兼容 Windows 交互
- 是否有可拆分但未拆分的长组件
- 是否优先复用了 `assets/icons`
- 是否存在 analyze warning
- 是否存在可直接修掉的低级 lint 问题
- 是否新增了命名不清晰或职责不清晰的代码

如果以上任一项不满足，先修正，再交付。
