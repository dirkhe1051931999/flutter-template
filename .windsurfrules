# Flutter/Dart Coding Rules for Vue/TypeScript Developers

本项目的代码风格应尽量贴近高级前端开发者熟悉的 Vue + TypeScript 工程化思维：模块职责清晰、类型显式、单例集中、API 层薄封装、状态不可变、命名语义化。

## 总原则

- 写 Dart/Flutter 代码时，优先类比 Vue/TS 项目中的 `request.ts`、`api/*.ts`、`store/*.ts`、`types.ts`、`constants.ts` 分层方式。
- 避免把初始化、副作用、业务逻辑、UI 构建混在同一个 Widget 中。
- 避免使用 `dynamic`、裸 `Map<String, dynamic>` 作为核心业务模型；优先使用强类型 class/model。
- 公共实例应集中导出，不要在多个业务文件里重复 `new`。
- 命名必须有业务语义，不允许长期保留 demo 命名，例如 `path1`、`testPage`、`data1`。

## 网络层规则

### 错误写法

```dart
var client = DioClient(baseUrl: 'https://example.com');
```

在每个 API 文件中重复创建 client，等价于在多个 Vue API 文件里重复 `axios.create()`，会导致拦截器、baseURL、token、cookie 行为不一致。

### 正确写法

```dart
import 'package:flutter_template_start/utils/request.dart';

final response = await httpClient.get('/guestbook/list');
```

统一从 `utils/request.dart` 导入 `httpClient`，类似 Vue/TS 项目中的统一 `request.ts` 实例。

### 规则

- `DioClient` 只能在网络基础设施层创建。
- API 文件只负责声明业务接口，不负责创建 HTTP client。
- token 注入、refresh token、错误日志、cookie 管理必须放在 `DioClient` 拦截器中。
- 不允许在页面 Widget 中直接调用 `Dio()` 或 `DioClient()`。

## API 层规则

### 错误写法

```dart
dynamic commonGetRequest() async {
  final response = await httpClient.get('/guestbook/list');
  return response.data;
}
```

问题：返回类型不明确，调用方不知道数据结构。

### 正确写法

```dart
Future<ListModel?> getNewsList(Map<String, dynamic> params) async {
  final response = await httpClient.get('/guestbook/list', queryParameters: params);
  return ListModel.fromJson(response.data);
}
```

### 规则

- API 方法必须使用明确返回类型，例如 `Future<UserModel?>`、`Future<List<NewsItem>>`。
- API 方法名要表达业务含义，例如 `getNewsList`，不要使用 `commonGetRequest` 这类泛化命名。
- API 层负责把 response 转成 model，不把裸 `response.data` 随意透传到 UI 层。

## 类型和 Model 规则

### 错误写法

```dart
Map<String, dynamic> user = {
  'name': 'Tom',
  'age': 18,
};
```

核心业务数据长期使用 Map，类似 TS 里全部写 `any`。

### 正确写法

```dart
class UserInfo {
  const UserInfo({
    required this.name,
    required this.age,
  });

  final String name;
  final int age;
}
```

### 规则

- 业务实体必须建 model class。
- 能不用 `dynamic` 就不用 `dynamic`。
- `fromJson` / `toJson` 统一放在 model 内。
- 命名不要使用 TypeScript 接口风格的 `IUserInfo`，Dart 中使用 `UserInfo`。

## 状态管理规则

### 错误写法

```dart
AppState update(dynamic changes) {
  return AppState._(recursiveMerge(_data, changes));
}
```

问题：类似 TS 中用 `any` 改 store，类型不可控。

### 正确写法

```dart
class AppState {
  const AppState({
    required this.todos,
    required this.userInfo,
  });

  final List<String> todos;
  final UserInfo userInfo;

  AppState copyWith({
    List<String>? todos,
    UserInfo? userInfo,
  }) {
    return AppState(
      todos: todos ?? this.todos,
      userInfo: userInfo ?? this.userInfo,
    );
  }
}
```

### 规则

- Store state 应使用强类型不可变对象。
- 修改 state 时使用 `copyWith` 思路，类似前端 immutable update。
- Reducer/action 必须有明确类型，不要用无约束 `dynamic action`。

## Widget 组件规则

### 错误写法

```dart
class Layout extends StatelessWidget {
  Layout() {
    initRouter();
    initStore();
    initLoading();
  }
}
```

问题：Widget 构造函数里做过多副作用，类似在 Vue render 阶段做应用初始化。

### 正确写法

```dart
void main() {
  final router = createRouter();
  final store = createStore();

  runApp(App(router: router, store: store));
}
```

### 规则

- Widget 主要负责 UI composition。
- 应用启动初始化放在 `main()` 或 bootstrap 文件中。
- 页面 Widget 不直接承担网络 client、router、store 初始化职责。
- 可复用 UI 抽成 components，业务页面放 pages。

## 命名规则

### 错误写法

```dart
static String path1 = '/path1';
static String path2 = '/path2';
var data1 = {};
class IUserinfo {}
```

### 正确写法

```dart
static const newsDetail = '/news/detail';
static const todoList = '/todolist';
final newsQuery = NewsQuery(...);
class UserInfo {}
```

### 规则

- 类名使用 `UpperCamelCase`。
- 变量、方法、字段使用 `lowerCamelCase`。
- 路由名、API 方法名、model 名必须表达业务语义。
- 不使用 `I` 前缀模拟 TypeScript interface。

## 配置规则

### 错误写法

```dart
class APP_CONFIG {
  static const String BASE_URL = 'https://example.com';
}
```

### 正确写法

```dart
class AppConfig {
  static const baseUrl = String.fromEnvironment(
    'BASE_URL',
    defaultValue: 'https://example.com',
  );
}
```

### 规则

- 配置类使用 Dart 风格命名，例如 `AppConfig`。
- 环境配置优先考虑 `--dart-define`。
- 不要为了保留错误命名而添加 lint ignore。

## 错误处理规则

### 错误写法

```dart
try {
  await request();
} catch (e) {
  print(e);
}
```

### 正确写法

```dart
try {
  await request();
} catch (error, stackTrace) {
  customLogger.log('request failed: $error');
}
```

### 规则

- 不使用 `print` 做业务日志。
- catch 中优先保留错误上下文。
- 网络错误统一在 `DioClient` 中做基础日志和转换。
- UI 层只处理用户可见反馈。

## 目录结构约定

目标：让 Flutter 项目像 Vue/TS 项目一样“文件放哪里一眼就知道”。

### 规则

- `lib/utils/`：基础设施工具（例如 `request.dart`、`logger.dart`、`storage.dart`）。
- `lib/api/`：API 声明层，只负责声明接口 + 返回强类型 model，不写 UI。
- `lib/model/`：业务数据结构（entity/DTO），包含 `fromJson/toJson`。
- `lib/store/`：全局状态（redux 的 state/action/reducer），保持 immutable。
- `lib/pages/`：页面（路由入口），只做 UI composition，不直接写复杂业务。
- `lib/components/`：可复用组件（尽量无业务或轻业务）。
- `lib/router/`：路由与跳转封装（等价 Vue 的 `router/index.ts`）。
- `lib/constants/`：常量/枚举/配置 key（等价 `constants.ts`）。

## UI / ViewModel 规则

目标：页面代码保持“像 Vue 的 template + computed”，只消费稳定结构，不直接吃后端原始 JSON。

### 规则

- 页面 Widget（`pages/**`）中不允许出现：
  - `Map<String, dynamic>` 作为业务数据承载
  - `json['xx']` 这类 JSON 字段直取
  - `response.data['xx']` 这类对网络层返回的直连渲染
- API 层必须把响应转换成 model（你上面已定义），页面层只消费：
  - model
  - 或者更进一步：页面专用 ViewModel（推荐用 `xxx_view_model.dart`/`mapper` 负责转换）

## 副作用与生命周期规则

目标：避免“渲染期副作用”，等价于 Vue 里禁止在 render/template 执行请求。

### 规则

- `build()` 必须是纯 UI 渲染：给定当前 state -> 返回 widget tree。
- 不允许在 `build()` 中：
  - 发起网络请求
  - `store.dispatch(...)`
  - 读写本地存储（会导致重复触发、难以追踪）
- 允许的副作用位置：
  - `initState` / `didChangeDependencies`
  - 用户交互回调（`onPressed`/`onTap`）
  - 业务 controller/service（如果项目引入对应抽象）

## 单例与依赖创建规则

目标：对齐 Vue/TS 的“单例集中创建”，避免到处 `new` 造成行为不一致。

### 规则

- 只允许在 `main.dart` / `bootstrap.dart` 创建：router、store（全局单例）。
- 只允许在基础设施层（例如 `utils/request.dart`）创建并导出：`httpClient` / `DioClient` / logger。
- 业务页面/组件中不允许直接 `new`：
  - `Dio()` / `DioClient()`
  - `FluroRouter()`
  - `Store()`

## 分层 import 规则（禁止倒引用）

目标：像前端工程一样，避免跨层耦合和循环依赖。

### 规则

- `model/**` 不允许 import：`api/**`、`pages/**`、`store/**`。
- `api/**` 不允许 import：`pages/**`。
- `components/**` 不允许 import：具体业务页面（避免组件绑定页面）。
- `pages/**` 可以组合其它层，但应避免页面之间互相 import 形成网状依赖。

## 统一返回与错误模型（建议）

目标：让调用方像 TS 里消费 `Response<T>` 一样稳定，UI 只处理 success/failure。

### 规则

- API 层不建议把三方异常（例如 Dio 的 error 类型）原样抛给 UI。
- 建议在网络基础设施层将错误统一转换为项目内错误类型（例如 `RequestError`），并在 API 层对外返回：
  - `ApiResult<T>`（成功/失败 + message + code + data）
  - 或明确的 `T?` + 统一异常（两者选一种，项目内保持一致）

## 后续改代码时必须遵守

- 如果要新增 API，先放到 `lib/api/**`，使用统一 `httpClient`。
- 如果要新增接口返回数据，先建 model，再写 API 方法。
- 如果发现重复 client、重复 baseURL、重复 token 处理，应优先收敛到基础设施层。
- 如果 Dart 写法和 Vue/TS 思路冲突，优先选择 Dart 生态推荐写法，但解释时使用 Vue/TS 类比说明。
